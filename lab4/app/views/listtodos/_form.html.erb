<%= form_for(@listtodo) do |f| %>
  <% if @listtodo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@listtodo.errors.count, "error") %> prohibited this listtodo from being saved:</h2>

      <ul>
      <% @listtodo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :todo %><br>
    <%= f.text_field :todo %>
  </div>
  <div class="field">
    <%= f.label :due %><br>
    <%= f.text_field :due %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
